/**
 * This package contains the interfaces required for the socket middleware.
 * <p>
 * Right now it contains the {@link distibuted.socket.middleware.interfaces.SocketObject} interface that identifies the methods that an object sent via socket must implement
 */
package distibuted.socket.middleware.interfaces;